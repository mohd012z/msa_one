import fs from 'node:fs';import assert from 'node:assert/strict';const js=fs.readFileSync('www/create-studio.js','utf8'),css=fs.readFileSync('www/create-studio.css','utf8');for(const k of ['renderDocument','renderHtml','renderSheet','renderSlides','renderPdf','.csv','sandbox','data-device','Native PDF export is not enabled yet'])assert.ok(js.includes(k),'missing format feature '+k);for(const k of ['studio-sheet','studio-slide','studio-preview-wrap'])assert.ok(css.includes(k),'missing format style '+k);console.log('create format contracts passed');