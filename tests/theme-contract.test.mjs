import fs from 'node:fs';import assert from 'node:assert/strict';const css=fs.readFileSync('www/theme-v2.css','utf8');const js=fs.readFileSync('www/settings-v2.js','utf8');assert.ok(css.includes('.page{background:linear-gradient'),'page canvas must use dark gradient');for(const k of ['soft','round','pill','compact','ios'])assert.ok(js.includes(k),'missing shape '+k);assert.ok(js.includes('localStorage.setItem'),'shape must persist');console.log('theme contract passed');