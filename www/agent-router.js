(()=>{function audit(agentId,action,result,detail){return window.MSAAgentAudit?.record(agentId,action,result,detail)}function authorize(agentId,tool){return!!window.MSAAgentRegistry?.authorize(agentId,tool)}function route(agentId,tool,payload){const agent=window.MSAAgentRegistry?.get(agentId);if(!agent){audit(agentId,tool,'DENIED','Unknown agent');return{ok:false,status:'DENIED',reason:'Unknown agent'}}if(!authorize(agentId,tool)){audit(agentId,tool,'DENIED','Tool not permitted');return{ok:false,status:'DENIED',reason:'Tool not permitted',agent}}audit(agentId,tool,'AUTHORIZED');return{ok:true,status:'AUTHORIZED',agent,tool,payload:payload??null}}window.MSAAgentRouter={route,authorize,audit};})();