(()=>{const KEY='msaOneProjectsV1';let state={type:'document',id:null,timer:null};const TYPES={document:['📄','Document'],spreadsheet:['📊','Spreadsheet'],presentation:['📽️','Presentation'],pdf:['📕','PDF'],html:['🌐','Smart HTML']};
function all(){try{return JSON.parse(localStorage.getItem(KEY)||'[]')}catch{return[]}}function put(p){let a=all(),i=a.findIndex(x=>x.id===p.id);i<0?a.unshift(p):a[i]=p;localStorage.setItem(KEY,JSON.stringify(a.slice(0,30)));return p}function get(id){return all().find(x=>x.id===id)}function esc(s=''){return s.replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}
function wireTiles(){document.querySelectorAll('#create .tile,#home .tile,#ai .tile').forEach(b=>{let t=(b.querySelector('strong')?.textContent||'').toLowerCase(),type=t.includes('spreadsheet')?'spreadsheet':t.includes('presentation')?'presentation':t.includes('smart html')?'html':t.includes('pdf')?'pdf':t.includes('document')?'document':null;if(type)b.onclick=()=>open(type)})}
function mount(){if(document.querySelector('.studio-overlay')){wireTiles();return}let x=document.createElement('section');x.className='studio-overlay';x.innerHTML='<header class="studio-top"><button data-close>‹</button><div class="studio-title"><input data-title value="Untitled"><div class="studio-status">Ready · saved on this device</div></div><button class="studio-primary" data-export>Export</button></header><div class="studio-body"><div class="studio-mode">'+Object.entries(TYPES).map(([k,v])=>`<button class="studio-type" data-type="${k}">${v[0]}<br>${v[1]}</button>`).join('')+'</div><div data-work></div><div class="studio-recents" data-recents></div></div>';document.body.appendChild(x);x.querySelector('[data-close]').onclick=close;x.querySelector('[data-export]').onclick=exportCurrent;x.querySelector('[data-title]').oninput=queueSave;x.querySelectorAll('[data-type]').forEach(b=>b.onclick=()=>open(b.dataset.type));wireTiles();renderRecents()}
function render(){let x=document.querySelector('.studio-overlay'),w=x.querySelector('[data-work]');x.querySelectorAll('[data-type]').forEach(b=>b.classList.toggle('on',b.dataset.type===state.type));let p=state.id&&get(state.id);x.querySelector('[data-title]').value=p?.title||'Untitled '+TYPES[state.type][1];if(state.type==='document'){w.innerHTML='<div class="studio-tools"><button class="studio-tool" data-cmd="bold"><b>B</b></button><button class="studio-tool" data-cmd="italic"><i>I</i></button><button class="studio-tool" data-cmd="insertUnorderedList">☷ List</button></div><article class="studio-editor" contenteditable="true" data-doc>'+(p?.content||'<h2>Start writing</h2><p>Your draft autosaves on this device.</p>')+'</article>';w.querySelector('[data-doc]').oninput=queueSave;w.querySelectorAll('[data-cmd]').forEach(b=>b.onclick=()=>{document.execCommand(b.dataset.cmd);queueSave()})}else if(state.type==='html'){let src=p?.content||'<!doctype html>\n<html>\n<head><meta name="viewport" content="width=device-width"><title>MSA One</title></head>\n<body>\n  <h1>Hello from MSA One</h1>\n  <p>Edit the source and preview it below.</p>\n</body>\n</html>';w.innerHTML='<textarea class="studio-code" data-code></textarea><iframe class="studio-preview-frame" data-preview sandbox></iframe>';let c=w.querySelector('[data-code]');c.value=src;c.oninput=()=>{preview();queueSave()};preview()}else w.innerHTML='<div class="studio-empty"><b>'+TYPES[state.type][0]+' '+TYPES[state.type][1]+' workspace prepared</b><p>This module is wired into Create Studio, but native '+TYPES[state.type][1]+' generation is not enabled yet.</p><p class="studio-status">Your Create Studio architecture is ready for the format engine.</p></div>';renderRecents()}
function preview(){let c=document.querySelector('[data-code]'),f=document.querySelector('[data-preview]');if(c&&f)f.srcdoc=c.value}function currentContent(){return state.type==='document'?(document.querySelector('[data-doc]')?.innerHTML||''):state.type==='html'?(document.querySelector('[data-code]')?.value||''):''}
function saveDraft(){if(!['document','html'].includes(state.type))return;let title=document.querySelector('[data-title]')?.value.trim()||'Untitled';if(!state.id)state.id='p_'+Date.now().toString(36);put({id:state.id,type:state.type,title,content:currentContent(),updated:Date.now()});let s=document.querySelector('.studio-status');if(s)s.textContent='Saved · '+new Date().toLocaleTimeString([],{hour:'2-digit',minute:'2-digit'});renderRecents()}function queueSave(){clearTimeout(state.timer);state.timer=setTimeout(saveDraft,350)}
function open(type='document',id=null){mount();state.type=TYPES[type]?type:'document';state.id=id;document.querySelector('.studio-overlay').classList.add('on');render()}function close(){saveDraft();document.querySelector('.studio-overlay')?.classList.remove('on')}function openProject(id){let p=get(id);if(p)open(p.type,p.id)}
function renderRecents(){let r=document.querySelector('[data-recents]');if(!r)return;let a=all().slice(0,4);r.innerHTML=a.length?'<div class="studio-status">RECENT DRAFTS</div>'+a.map(p=>`<div class="studio-recent"><span>${TYPES[p.type]?.[0]||'◆'}</span><div><b>${esc(p.title)}</b><div class="studio-status">${new Date(p.updated).toLocaleString()}</div></div><button class="studio-tool" data-open="${p.id}">Open</button></div>`).join(''):'';r.querySelectorAll('[data-open]').forEach(b=>b.onclick=()=>openProject(b.dataset.open))}
function download(name,data,type){let u=URL.createObjectURL(new Blob([data],{type})),a=document.createElement('a');a.href=u;a.download=name;a.click();setTimeout(()=>URL.revokeObjectURL(u),500)}function exportCurrent(){saveDraft();let title=(document.querySelector('[data-title]')?.value||'msa-one').replace(/[^\w-]+/g,'-');if(state.type==='html')download(title+'.html',currentContent(),'text/html');else if(state.type==='document')download(title+'.html','<!doctype html><meta charset="utf-8"><title>'+esc(title)+'</title>'+currentContent(),'text/html');else alert(TYPES[state.type][1]+' export engine is not enabled yet.')}
window.MSAStudio={open,close,saveDraft,exportCurrent};document.addEventListener('DOMContentLoaded',mount);setTimeout(mount,400)})();